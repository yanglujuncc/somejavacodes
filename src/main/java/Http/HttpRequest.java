package Http;

import java.nio.ByteBuffer;

public class HttpRequest {
	ByteBuffer writeMessageHeadBuffer=ByteBuffer.allocate(1024);
	ByteBuffer writeMessageBodyBuffer=ByteBuffer.allocate(1024);
	
	
}
